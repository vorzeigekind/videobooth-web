// importing settings from config.json
import config from './config.json' assert { type: 'json' };
console.log(data);

// FUNCTION > change visibility of target-ID, visible/hidden, if hidden > direction to move
/*function changeVis(target, visibility, direction) {
    var queryTarget = document.querySelector('#' + target);
    if (visibility == 'visible') {
        queryTarget.classList.remove('hidden');
        if (queryTarget.classList.contains('direction-up');)
        queryTarget.classList.add('visible');
    }
}*/



// element.classList.contains(class);